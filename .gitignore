# Skip nested duplicate extraction artifact
K BNG M Hoster V.0.2 - fix/

# Runtime folders
Backups/
Logs/
Quarantine/

# User's own webhook URL (never commit real URLs)
webhook.txt

# Local secrets (never commit your BeamMP AuthKey)
.env
